{
  "name": "pages",
  "dependencies": {
    "wgpu-matrix": "^3.4.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@webgpu/types": "^0.1.60"
  },
  "peerDependencies": {
    "typescript": "^5.7.3"
  },
  "scripts": {
    "predev": "bun run prepare.ts",
    "dev": "bun run --hot dev.ts"
  }
}